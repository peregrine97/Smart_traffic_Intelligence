# Smart Traffic Intelligence — Backend Development & Testing Dependencies
# Install with: pip install -r backend/requirements-dev.txt
# (Also requires: pip install -r backend/requirements.txt)

# ── Testing Framework ──────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# ── HTTP Test Client (ASGI) ────────────────────────────────────────────────
httpx>=0.27.0

# ── Mocking ───────────────────────────────────────────────────────────────
responses>=0.25.0          # Mock requests library HTTP calls

# ── Security Scanning ─────────────────────────────────────────────────────
pip-audit>=2.7.0

# ── Code Quality ──────────────────────────────────────────────────────────
ruff>=0.4.0                # Fast Python linter (replaces flake8)
